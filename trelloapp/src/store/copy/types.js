/**
 * State: Redux 스토어에서 관리할 상태의 구조입니다.
 * 자바스크립트에서는 특정 타입을 강제하지 않으므로 
 * 초기값 설정이나 문서화 용도로 활용합니다.
 */
export const initialState = {};

/**
 * Actions: Dispatch할 액션 객체의 기본 구조입니다.
 * 보통 { type: string, payload?: any } 형태를 가집니다.
 */